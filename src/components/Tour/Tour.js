import React, { Component } from 'react'

export default class Tour extends Component {
    render() {
        return (
            <section>
                tour
            </section>
        )
    }
}